# Variables d'environnement pour Phases 3, 4, 5
# À copier dans votre fichier .env

# ========================================
# PHASE 5 : CINETPAY (Paiements)
# ========================================

# URLs API CinetPay
CINETPAY_API_URL=https://api-checkout.cinetpay.com/v2

# Credentials CinetPay (À obtenir sur https://cinetpay.com)
# Mode TEST : Utiliser les credentials de test
# Mode PRODUCTION : Utiliser les credentials de production
CINETPAY_SITE_ID=your-site-id-here
CINETPAY_API_KEY=your-api-key-here
CINETPAY_SECRET_KEY=your-secret-key-here

# Webhooks et URLs de retour
CINETPAY_NOTIFY_URL=${APP_URL}/api/webhooks/cinetpay
CINETPAY_RETURN_URL=${APP_URL}/payment/callback

# Mode : TEST ou PRODUCTION
CINETPAY_MODE=PRODUCTION

# ========================================
# PHASE 3 : AWS S3 (Stockage photos)
# ========================================

# AWS Credentials
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_DEFAULT_REGION=us-east-1

# S3 Bucket
AWS_BUCKET=pouire-photos
AWS_URL=https://pouire-photos.s3.amazonaws.com
AWS_USE_PATH_STYLE_ENDPOINT=false

# Filesystem disk (s3 pour production, local pour dev)
FILESYSTEM_DISK=s3

# ========================================
# FRONTEND URL
# ========================================

# URL du frontend pour les redirections après paiement
FRONTEND_URL=https://pouire.com

# ========================================
# QUEUE CONFIGURATION (Pour Jobs asynchrones)
# ========================================

# Driver de queue : redis recommandé pour production
QUEUE_CONNECTION=redis

# Redis configuration
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# ========================================
# EMAIL CONFIGURATION
# ========================================

# Configuration SendGrid (recommandé) ou autre SMTP
MAIL_MAILER=smtp
MAIL_HOST=smtp.sendgrid.net
MAIL_PORT=587
MAIL_USERNAME=apikey
MAIL_PASSWORD=your-sendgrid-api-key
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@pouire.com
MAIL_FROM_NAME="${APP_NAME}"

# ========================================
# INFORMATIONS ADDITIONNELLES
# ========================================

# Moyens de paiement supportés via CinetPay :
# - Orange Money Burkina (ORANGE_MONEY_BF)
# - MTN Money Burkina (MTN_MONEY_BF)
# - Moov Money Burkina (MOOV_MONEY_BF)
# - Wave Burkina (WAVE_BF)
# - Carte bancaire (CARD)

# Devise : XOF (Franc CFA)
# Prix minimum : 500 FCFA
# Commission plateforme : 20%
# Commission photographe : 80%

# URLs de téléchargement : Signées, expiration 24h

# Structure S3 :
# photos/{photographer_id}/originals/{uuid}.jpg (privé)
# photos/{photographer_id}/previews/{uuid}.jpg (public, avec watermark)
# photos/{photographer_id}/thumbnails/{uuid}.jpg (public, 400x300)
# users/{user_id}/avatars/{uuid}.jpg (public)
# users/{user_id}/covers/{uuid}.jpg (public)
# invoices/invoice-{order_number}.pdf (privé)
